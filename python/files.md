# Files // Файлы 


```
    f = open('data.txt', 'w')  # Cоздает новый файл для вывода (w - означает write)
    f = open('data.txt', 'r')  #  Открывает файл для чтения (к - означает write)
    f = open('data.txt')  # просто открывает файл (ну фиг знает, типа он открыт, наслаждайся)

    f.write('qwe \n')  # запись строки байтов в файл 
    f.write('word \n')  # запись строки байтов в файл 
    f.writelines(L)  # запись строк в файл

    text = f.read()  # файл читается целиком
    print(text)
    >>>> qwe
    >>>> word

    text = f.readline()  # чтение строки
    text = f.readlines()  # чтение файла целиком в список строк

    f.close()  # закрывает файл и выталкивает выходные буферы на диск
    f.flush()  # выталкивает выходные буферы на диск
```